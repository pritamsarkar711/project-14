'use strict';
function validateXml(xml, type='urlset') { const errors=[]; if(!/^<\?xml\s+version="1\.0"\s+encoding="UTF-8"\?>/.test(xml.trim())) errors.push('Missing XML declaration.'); if(type==='urlset'&&!/<urlset\b[^>]*sitemaps\.org\/schemas\/sitemap\/0\.9/.test(xml)) errors.push('Missing urlset namespace.'); if(type==='index'&&!/<sitemapindex\b[^>]*sitemaps\.org\/schemas\/sitemap\/0\.9/.test(xml)) errors.push('Missing sitemapindex namespace.'); if(/[\u0000-\u0008\u000b\u000c\u000e-\u001f]/.test(xml)) errors.push('Invalid control characters.'); const locs=[...xml.matchAll(/<loc>([\s\S]*?)<\/loc>/g)].map(m=>m[1]); const seen=new Set(); for(const l of locs){ const un=l.replace(/&amp;/g,'&').replace(/&lt;/g,'<').replace(/&gt;/g,'>').replace(/&quot;/g,'"').replace(/&apos;/g,"'"); try{ const u=new URL(un); if(!/^https?:$/.test(u.protocol)) errors.push('Invalid URL protocol in loc.'); }catch{ errors.push('Invalid URL in loc.'); } if(seen.has(l)) errors.push('Duplicate loc: '+l); seen.add(l); } return { valid: errors.length===0, errors, locCount: locs.length, bytes: Buffer.byteLength(xml,'utf8') }; }
module.exports={validateXml};
