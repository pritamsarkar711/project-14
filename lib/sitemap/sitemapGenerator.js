'use strict';
const esc=s=>String(s).replace(/[&<>'"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&apos;','"':'&quot;'}[m]));
function locXml(page, opts={}) { let x='  <url>\n    <loc>'+esc(page.loc||page.url)+'</loc>\n'; if(page.lastmod) x+='    <lastmod>'+esc(page.lastmod)+'</lastmod>\n'; if(opts.changefreq) x+='    <changefreq>'+esc(opts.changefreq)+'</changefreq>\n'; if(opts.includePriority && page.priority != null) x+='    <priority>'+esc(page.priority)+'</priority>\n'; if(opts.includeImages && page.images && page.images.length){ for(const img of page.images) x+='    <image:image><image:loc>'+esc(img)+'</image:loc></image:image>\n'; } return x+'  </url>'; }
function generateUrlset(pages, opts={}) { const ns=opts.includeImages?' xmlns:image="http://www.google.com/schemas/sitemap-image/1.1"':''; return '<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"'+ns+'>\n'+pages.map(p=>locXml(p,opts)).join('\n')+'\n</urlset>\n'; }
function generateIndex(files, origin){ return '<?xml version="1.0" encoding="UTF-8"?>\n<sitemapindex xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n'+files.map(f=>'  <sitemap>\n    <loc>'+esc(new URL('/'+f.name, origin).toString())+'</loc>\n  </sitemap>').join('\n')+'\n</sitemapindex>\n'; }
module.exports={generateUrlset,generateIndex};
